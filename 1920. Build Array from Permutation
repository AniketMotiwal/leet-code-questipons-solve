class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
       vector<int>result;
       result=nums;
       for(int i=0;i<nums.size();i++){
           nums[i]=result[result[i]];
       }
       return nums;
    }
   
};
